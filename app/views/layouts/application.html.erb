<!DOCTYPE html>
<html>
<head>
  <title><%= full_title(yield(:title)) %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <link rel="icon" type="image/png" href="https://school-diary-app-bucket.s3.ap-northeast-1.amazonaws.com/favicon-logo.png">
</head>

<body>
    <% if user_signed_in? %>
        <%= render 'layouts/header', school_code: current_user.grade_class&.school_code %>
    <% end %>
    <%= yield %>
</body>
</html>
